{
  "name": "settle-map",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "module": "lib/index.mjs",
  "types": "lib/index.d.ts",
  "private": false,
  "author": "Fahim Faisal",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts -d lib --clean --minify",
    "test": "vitest run",
    "lint": "tsc",
    "ci": "pnpm run lint && pnpm run test && pnpm run build",
    "release": "pnpm run ci && changeset publish"
  },
  "dependencies": {
    "p-limit": "^4.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
